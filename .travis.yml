language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-aspnet-item' ]; then cd .. && eval "mv $currentfolder generator-aspnet-item" && cd generator-aspnet-item; fi

